/*
export interface AnalyticsMenuConfig {
  id: string;
  icon?: string;
  label: string;
  /*edit: boolean;/
  visible?: boolean;
  children: AnalyticsMenuConfig[];
}*/

export interface DropInfo {
  targetId: string;
  action?: string;
}
