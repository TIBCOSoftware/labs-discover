import { ClaimsValues } from './claimsValues';
/**
 * Details supplied for the Claims
 */
export interface Claims extends ClaimsValues {
    /**
     * The unique identifier for a User
     */
    id: string;
}
